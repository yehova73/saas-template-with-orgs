const PoliciesLayout: React.FC<React.PropsWithChildren> = ({ children }) => {
  return (
    <section className="py-24 w-full flex justify-center">{children}</section>
  );
};

export default PoliciesLayout;
